type _Schema_
  @fulltext(
    name: "jf"
    language: EN
    algorithm: RANKED
    include: [
      {
        entity: "Musician",
        fields: [
          { name: 9 },
          { name: "bio" },
        ]
      }
    ]
)

enum __FulltextAlgorithm {
  RANKED
  PROXIMITY_RANKED
}

type B @entity {
  id: ID!
  foo: String!
}
