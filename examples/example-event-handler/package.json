{
  "name": "example-event-handler",
  "scripts": {
    "codegen": "graph codegen --output-dir types/ data-source.yaml",
    "build": "graph build data-source.yaml",
    "build-ipfs": "graph build --ipfs /ip4/127.0.0.1/tcp/5001 data-source.yaml",
    "build-wast": "graph build -t wast data-source.yaml"
  },
  "devDependencies": {
    "graph-cli": "ssh://git@github.com/graphprotocol/graph-cli"
  }
}
