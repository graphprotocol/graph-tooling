{
  "name": "example-event-handler",
  "scripts": {
    "codegen": "graph codegen --output-dir types/ package.yaml",
    "build": "graph build package.yaml",
    "build-ipfs": "graph build --ipfs /ip4/127.0.0.1/tcp/5001 package.yaml",
    "build-wast": "graph build -t wast package.yaml"
  },
  "devDependencies": {
    "graph-cli": "ssh://git@github.com/graphprotocol/graph-cli"
  }
}
